/* * EuclideanNorm.java * Class implementing the euclidean vector norm. */package numeric;import numeric.VectorNorm;/** * Implements the euclidean vector norm, aka L2 norm. * @author Pasquale Foggia * @version 0.99, 18 Dec 1997 */public class EuclideanNorm implements VectorNorm  {    /**     * Returns the euclidean vector norm of the vector x,     * defined as:<P>     *    sqrt(x[0]**2+...+x[n-1]**2)     */    public double norm(double x[])      { int i=x.length;        double sum=0.0;        while (--i >= 0)          sum+=x[i]*x[i];        return Math.sqrt(sum);       }  }