package ui;import util.*;import numeric.*;import java.awt.*;/** * A canvas displaying a set of GraphicDisplay * @author Francesco Delfino * @version 0.99, Dec 1998 */public class MultiGraphicDisplay extends Panel {    GraphicDisplay display[];        int num=0;       public MultiGraphicDisplay() {            display=null;    }        public void setNumberOfDisplay (int _num)        {        Dimension d=size();        int i;        removeAll();        num=_num;                display=new GraphicDisplay[num];        setLayout(new VerticalLayout());        //todo: delete previuos graphics        for (i=0; i<num; i++) {            display[i]=new GraphicDisplay();            display[i].setSize(d.width,d.height/num);            add(display[i]);            //display[i].setVisible(true);            //System.out.println("Ho preparato ");        }            }        public void setStatusDisplayer(StatusDisplayer a)    {        int i;        for (i=0; i<num;i++)            display[i].setStatusDisplayer(a);           }   public GraphicDisplay getDisplay (int num)        {        return display[num];        }   public void autoResizeItems()        {         int i;         Dimension d=getParent().size();         for (i=0; i<num;i++)           { display[i].setSize(d.width,d.height/num);           // display[i].setVisible(true);           }        }}